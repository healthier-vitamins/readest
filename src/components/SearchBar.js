import { Form } from "react-bootstrap";

function SearchBar() {
  return (
    <div>
      <Form.Control type="text" placeholder="Search word"></Form.Control>
    </div>
  );
}

export default SearchBar;
